
{
  "name": "tankhan-io",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=main.js --format=esm --jsx=automatic --minify --platform=browser --target=es2020 --define:process.env.NODE_ENV=\\\"production\\\" --define:process.env.API_KEY=\\\"\\\" --external:react --external:react/* --external:react-dom --external:react-dom/* --external:lucide-react --external:@google/genai"
  }
}
